# GraphHopper 7.0 import config for prebuilt offline routing graphs.
# Must match the profile defined in RoutingManager.carProfile().
graphhopper:
  profiles:
    - name: car
      vehicle: car
      weighting: fastest

  graph.elevation.provider: noop
